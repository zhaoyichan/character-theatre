# 小剧场（character-theatre）· AI 开发经历与问题处置全记录

> 本文档由负责 v2.0.0 开发对接的 AI（助手）在 2026-09-04 编写，逐条记录了**本次从接手到发布全程中遇到的所有问题、排查思路、根因、解决与验证**。
> **给下一位接手的开发者**：这是比「开发日志.md」更细的问题排障底稿，遇到同款症状先来这里对照。

---

## 〇、接手时的心得（先读）

这次开发最大的教训就一条：**改「真身运行文件」时，绝对不要依赖沙箱/工作区的 edit_file 工具做多轮拼接**——它会在读写之间发生 desync，把文件尾部搞出重复乱码。**正确姿势：用 `super_admin:shell` 直改磁盘真身 + `node --check` 全程守门 + `cp` 同步三处。**

本插件所有关键文件须三处同步：
| 用途 | 路径 |
|---|---|
| 运行目录（酒馆真身，需 root 写） | `/storage/emulated/0/Android/data/com.tauritavern.client/data/extensions/third-party/character-theatre/` |
| 开发镜像 | `/storage/emulated/0/Download/酒馆插件开发/酒馆小剧场开发/character-theatre/` |
| 需求铁律 | `/storage/emulated/0/Download/酒馆插件开发/liwe记忆/需求-小剧场功能.md` |

改完必须**完全冷启动酒馆**（只刷新无效）；Android data 目录普通 terminal 读不了，必须 `super_admin:shell` 的 `cp`。

---

## 一、问题 1：沙箱 edit 工具损坏 `index.js` 文件尾（最严重）

- **症状**：对 `registerEntry` 做编辑后，`index.js` 尾部（原 1869 行的文件）出现**大量重复粘贴的乱码**——`})();ry(); return; }`、`navigator.clipboard...`、重复的 `if (document.readyState...)` 块叠了 3 遍，整个文件行数从 1869 膨胀到 1870+，`node --check` 直接报 SyntaxError。
- **排查**：用 `super_admin:terminal` 的 `wc -l`/`tail`/`grep` 直读磁盘文件，确认损坏**确实在磁盘真身**（不是工具缓存假象）。并发现磁盘开发镜像 `index.js`(110259B/1869行)、沙箱 `repo:小剧场` 的 `character-theatre/index.js` 是**两份不同拷贝**。
- **根因**：沙箱 edit_file 的 `old` 字符串匹配失败时静默产生局部替换，且多次编辑时对 `})();` 这类 IIFE 结束符周围做字符串替换极易把尾部拖乱；加上 read/edit 之间缓存不同步。
- **解决**：
  1. 用 `head -n 1838` 截取干净前段 + `printf '\n})();\n'` 拼接正确结尾，生成 **1840 行干净版** `_th_fixed2.js`；
  2. `cp` 覆盖真身；
  3. `node --check` 验证 **SYNTAX_OK**（dev + 运行目录都已验）。
- **验证**：`node --check` 通过；关键函数 `mesCornerBtn/favEnsureBtns/scanMesForFav` 计数正常。
- **教训**：**大文件改动禁止用沙箱 edit_file 反复拼**，一律改到 temp 文件 + Python 按行区间 splice（或 shell 直写），再 cp 真身 + node --check。

---

## 二、问题 2：收藏界面皮肤没对齐小剧场主界面

- **症状**：收藏查看界面（`#th-favroot`/`#th-favwin`）还是旧的 iOS 白色卡片风（纯白 `#fff`、灰 `#f0f0f2`、13px 圆角、44px 顶栏、`#9a9a9e`），跟小剧场主界面的「米白+深藏青+金色、16px 圆角、50px 顶栏」完全脱节。
- **解决**：把 `style.css` 里 `#th-favroot` 全部硬编码色改走小剧场 CSS 变量（`--th-bg --th-panel --th-card --th-ink --th-accent --th-gold --th-line`），圆角 13→16、顶栏 44→50px、勾选框选中小剧场深藏青+米白勾、阅读页卡片带轻阴影；并把 JS 里 `#th-favwin` 的 inline 圆角 20→16、背景/边框改 `var(--th-bg,#f6f5f1)` 等带 fallback。
- **验证**：`grep` 确认 `#f0f0f2` 硬编码清零、`var(--th-bg)` 引用出现。

---

## 三、问题 3：拍照按钮位置做错（与悬浮球并列 → 主人要求每条消息右下角）

- **症状**：我最初把「拍照/复制」做成**屏幕右下角与悬浮球垂直并列**的两个小圆钮。主人明确指出：**要放在每一条聊天消息右下角，与 ★收藏/✓收录同一位置，且作用于「这一条」消息**。
- **解决**：
  1. 新增通用角标注入 `mesCornerBtn(mes, opts)`；
  2. 在 `favEnsureBtns` 里把 **✓收录(2) ★收藏(50) 复制(74) 拍照(98)** 逐一注入每条 `.mes` 右下角；
  3. 移除 `registerEntry` 里 `thAddCorner('cam'/'copy')` 的屏幕并列调用；
  4. 重写 `thCopyOne(mes)`（复制这条）与 `thCaptureOne(mes)`（拍照这条）。
- **验证**：`grep` 确认旧函数 `thAddCorner/thCopyChat/thCaptureChat` 全部清零，新函数定义到位，`node --check` OK。

---

## 四、问题 4：拍照「图片保存失败」+ 拍摄方向/内容不对

- **症状**：点拍照要么报「这条消息图片导出失败」，要么出的是**横排、文字还截断**的图。
- **根因**：
  1. 「保存失败」= 原实现用 **SVG `<foreignObject>` → `<img>` → canvas** 一条路走到黑，而**安卓 WebView 对 foreignObject 栅格化进 canvas 经常失败**（`img.onerror` 或 `drawImage` 抛 SecurityError/乱码）。
  2. 「横/截断」= 兜底实现只 `fillText(ln.slice(0,72))` 硬截断，还是横向布局。
- **解决**（重写 `thCaptureOne`）：
  - **方案A**：离屏 `foreignObject` → canvas（宽 520 竖版 + `word-break`），能渲染出原样含图最好；
  - **方案B兜底**：`textFallback()` 纯文本 canvas，**竖屏**（宽 520、高随内容）+ `measureText` **按宽度自动逐词换行、显示全部文字**（不再截断）；
  - 全程 `logEvent('拍照-开始/成功/兜底/兜底-文本长度/成功-兜底/保存失败/异常')` 打点，供真机诊断。
- **验证**：`node --check` OK，`竖屏全文/拍照-兜底` 标记存在。

---

## 五、问题 5：收藏多选「注入到聊天」点了没用

- **症状**：收藏界面多选勾选后点「注入」，没有任何反应。
- **根因**：注入用的 `thConfirm → thModal` 生成的确认框是 `position:absolute; z-index:50` 且挂在小剧场面板 `getPanel()` 里；而收藏窗 `#th-favroot` 是 `position:fixed; z-index:999990` 盖满全屏 → **确认框被收藏窗遮罩压在底下**，根本看不到也点不到。
- **解决**：重写 `favInjectToChat`，改用**独立顶层确认框**（`position:fixed; z-index:9999999; 挂 document.body`，按钮 `#cj_cancel/#cj_ok`），并加全套 `注入-*` 日志（开始/无选择/匹配/取消/确认/数量/空文本/完成/异常）。注入逻辑：把选中收藏的每条消息一次性 `ctx.chat.push`（作为用户发言），`saveChat + reloadCurrentChat + emit MESSAGE_SENT`。
- **验证**：`node --check` OK；`favInjectToChat/cj_ok/注入-开始` 标记存在。主人真机确认**注入成功**。

---

## 六、问题 6：收藏多选「删除」也没用（同款坑）

- **症状**：注入修好后，主人反馈多选「删除」点了没用。
- **根因**：与问题 5 完全同理——删除确认仍用 `thConfirm`（挂 `getPanel()`、z-index:50），被收藏窗(999990)遮罩盖住。
- **解决**：新增**通用顶层确认 `favConfirmTop(title,text,yesText)`**（fixed + z:9999999 挂 body），把 `data-favdel` 处理器的 `thConfirm({...})` 换成 `favConfirmTop(...)`。
- **验证**：`node --check` OK；`favConfirmTop` 定义+调用到位，`thConfirm.*删除所选收藏` 残留清零。

---

## 七、问题 7：悬浮球不可拖动（新需求，顺利实现）

- **需求**：悬浮球可以长按拖动，还要记忆位置。
- **实现**（重写 `registerEntry`）：
  - `touch-action:none` 防滚动抢拖动；`touchstart/move/end` + `mousedown/move/up` 双支持；
  - `__dragMoved` 标志：位移 >8px 判定拖动，拖动后松手**不**误开面板，轻点才 `open()`；
  - `localStorage('th-fabpos')` 存 `{x,y}`，重启记忆；`clamp()` 边界保护 + `resize` 自适应。
- **验证**：`node --check` OK；主人确认「可以」。

---

## 八、问题 8：GitHub git 协议推不上（网络限制）

- **症状**：`git fetch/push` 到 `github.com` 一直超时（`git ls-remote` exit=124），但 `api.github.com`（REST HTTPS）通（200，0.6s）。
- **根因**：该网络环境对 `github.com` 的 **git smart-HTTP 端口**做限制，而 `api.github.com` 不受限。
- **解决**：改走 **GitHub REST API 提交**：
  1. 建本地 `/root/repo` 干净 git 工作区（避开 emulated 存储的 git 对象写入问题，根分区接近满也需注意）；
  2. 用 API 顺序执行：读 main HEAD → 逐个 `POST /git/blobs`（base64，用 `-d @file` 绕过参数过长）→ `POST /git/trees` → `POST /git/commits` → `PATCH /git/refs/heads/main`（force）。
- **验证**：远程 `main` 更新到 commit `de16e3f1dba9`，8 个文件全部确认推送成功。

---

## 九、问题 9：给文件配 .gitignore / 确认发布内容

- **解决**：`.gitignore` 含 `.DS_Store / Thumbs.db / *.bak`；明确发布内容为 index.js/style.css/manifest.json/README.md/开发日志.md/交接总纲/小剧场-交接文档/.gitignore 共 8 个文件。
- **注意**：交接用中文文件名（交接总纲-此刻全量.md / 小剧场-交接文档.md）在 git/API 里要按 UTF-8 原文处理，不要误转义。

---

## 十、给下一位的硬性提醒

1. **改大文件**：别用沙箱 edit_file，用 temp 文件 + Python splice 或 shell 直写，cp 真身 + `node --check`。
2. **弹窗**：凡是浮在收藏窗/聊天之上的确认、提示，一律挂 `document.body` + 顶层高 z-index（999999+），别用挂 panel 的 `thModal`。
3. **拍照**：安卓 WebView 里 foreignObject 不一定能画进 canvas，务必带纯文本 canvas 兜底，保证 PNG 一定产出。
4. **运行目录**：Android data 目录 terminal 读不了，写要走 `super_admin:shell`；`node --check` 先 cp 到 /sdcard/Download。
5. **冷启动酒馆**才加载新扩展。
6. **三处保持一致**（运行/开发/README等文档）。
7. **GitHub**：git 协议端不通就改 REST API 提交（blob→tree→commit→更新 ref）。

---

*本文件随新问题/新进展持续补充；与开发日志.md 互补，排查问题时先对照这里。*
