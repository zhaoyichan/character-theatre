# 开发经验与避坑记录 · 发送修复专档（2026-09-08）

> 本文档记录本次[发送到酒馆时灵时不灵+聊天崩成1条]踩过的所有坑、根因、误判、最终可靠解法。
> 下次再做任何往酒馆塞消息/渲染气泡/动聊天记录的功能，先读此文，别再掉坑。
> 结合 开发介绍.md、开发日志.md、开发经历与问题处置全记录.md 一并读。

---

## 一句话结论（先记死）

在 Taurus 定制客户端（柏宝箱）里，想让一条消息出现在聊天气泡，唯一可靠姿势：
`ctx.chat.push(...)` 写入 + `ctx.saveChat()` 落盘 + `injectMesBubble()`（克隆最后一条 .mes 气泡，换内容，插到 #chat 末尾）。

- 不要依赖 `reloadCurrentChat()` 去刷新出消息——它整段重绘，遇特定内容会把界面崩成只剩1条、甚至带角色名宏占位符。
- 也不要以为派发 `MESSAGE_SENT` 事件会自动上屏——这个客户端里它只入列不渲染，光靠它消息会发进去但看不见。

---
## 一、症状

插件发送按钮时灵时不灵；消息进记录但没气泡；或整个聊天崩成只剩一条。发送内容是带 $ 开头、超长、纯指令风格文本。

---

## 二、排查证据链

1. 埋诊断桩，sendText 逐步记录日志。
2. 真机日志：push 17 到 18 成功、saveChat ok、emit 派发，但 reload 一调，屏幕消息数从 10 崩到 1、末条出现角色名宏占位符。
3. 落盘铁证：直接读 jsonl 文件，确认 push 的消息确实写进文件（17 到 18 行）。证明消息进记录从没失败，问题只在渲染气泡这一步。
4. 对比实验：只靠 MESSAGE_SENT 事件，消息落盘但气泡不上屏；用 reloadCurrentChat，气泡能上屏但整段重绘崩坏。
5. 定案：injectMesBubble 克隆末条气泡、换内容、插入末尾并滚到底。只加一条，不依赖 reload 又稳定上屏，真机通过。

---

## 三、血泪坑

### 坑1 reloadCurrentChat 是崩界面元凶
- 它整段清空重绘，遇特定内容崩成只剩 1 条、甚至带未替换宏占位符。
- 对策：凡是一次只加一条消息的需求，一律用 injectMesBubble 增量插入，绝不整段 reload。

### 坑2 以为 MESSAGE_SENT 事件会自动渲染气泡
- 它在这个定制客户端里只入列不画气泡，光派发它消息会发进去但看不见。
- 对策：把 MESSAGE_SENT 当通知其它扩展用（保留无害），上屏交给 injectMesBubble 亲自做。

### 坑3 判定消息到没到屏，别只数 .mes 数量
- .mes 节点里还会算上楼层的楼层号、名字等子元素文本。判断上没上屏，要在最近 3 条 .mes 的整段文本里搜发送内容的特征片段，而不是只看末条前 20 个字符（那里通常是楼层号 #16 之类）。
- 之前就是只看末条前 24 字误判没上屏，多此一举调了 reload，反而把事情弄糟。

### 坑4 判定失败后做兜底刷新反而害事
- 一度在判定本条没上屏时去调 reloadCurrentChat 兜底，结果消息其实已上屏，被误判加上 reload 又搞崩。
- 对策：判定失败只记日志，绝不自动 reload。

### 坑5 手动 push 的消息对象字段要对齐酒馆 user 消息
- 带上 name、is_user、is_system、is_name、send_date、mes、swipes、swipe_info、swipes_total、swipe_cur、extra 等字段。
- 字段不全虽不致命，但对重绘、其它扩展可能造成参考不到，尽量对齐。

---
## 四、injectMesBubble 核心思路与调用

核心函数已实现在 index.js 里（搜索 injectMesBubble 即可找到完整代码）。思路：

1. 取 #chat 容器，拿到最后一条 .mes 气泡作模板。
2. cloneNode(true) 克隆一个全新气泡。
3. 往里找 .mes_text（或 .mes_block / .mes_content / .mes_textarea）这些放文字的节点，把整段文字换成要发送的内容。
4. 清掉克隆里可能残留的旧角标按钮、打上 data-th-injected 标记，避免和后续注入冲突。
5. appendChild 到 #chat 末尾，并把滚动容器滚到底。

调用姿势（sendText 和收藏注入 favInjectToChat 完全一致）：
对每条要发的消息：先生成一条标准 user 消息对象并 push 进 ctx.chat，全部 push 完后调用一次 ctx.saveChat() 落盘，再对每条内容调用一次 injectMesBubble 让它上屏。最后 emit 一次 MESSAGE_SENT 仅用于通知其它扩展（上屏已由 injectMesBubble 覆盖，不要再调 reloadCurrentChat）。

真正要记住的就三句：push 进数组 + saveChat 落盘 + injectMesBubble 上屏。三者缺一不可，顺序固定。

---

## 五、环境操作坑

### 坑6 沙箱 edit_file 会把超长 index.js 的文件尾搞坏
- 现象：多次编辑一个 2000 多行的 index.js 后，文件尾出现重复的结束符、孤立分号，或者某个函数（比如本来完整的函数）的闭合大括号被吞掉，导致 node 检查在文件尾报语法错误。
- 根因：在超大文件上反复用沙箱 edit 替换边界块，容易在收尾处错乱。
- 对策：
  1. 每次修改后立刻用 node 检查语法。
  2. 报错如果指向文件尾，先看最后十行，排查是不是多收拾尾符或少了闭合大括号。
  3. 可以用脚本统计目标某一段的花括号与圆括号净额，判断是否配平。
  4. 最省心：改大文件时直接在工作目录用 shell 复制覆盖或局部替换，别在沙箱里对超大文件反复打补丁。

### 坑7 reloadCurrentChat 别忘了别的地方也在用
- 一开始只改了发送功能，结果收藏注入功能里还留着一处同样的 reload 调用，有一样的崩界面隐患。
- 对策：凡是想让消息上屏、或出现了 reload 调用的地方，全局搜一遍一起改成 injectMesBubble。本次已全部清干净，真身里不再有 reload 的实际调用。

### 正确发布与同步流程（强烈建议下次照做）
1. 改开发副本目录里的文件。
2. 每次改完用 node 检查 JS 语法。
3. 需要生效时，先备份真身旧文件，再把开发副本复制到运行真身目录，两端 md5 必须一致。
4. 仓库、开发副本、运行真身三处保持一致。
5. 最后提醒用户完全冷启动酒馆，只刷新不生效。

---

## 六、数据目录访问速查（少折腾）

- 酒馆运行真身在 Android 应用私有 data 目录下，Ubuntu 桌面环境读不到，需要 Android 本机 root 或 Shizuku 权限才能读写。
- 聊天记录是 jsonl 文本文件，一行一条消息的 JSON。想确认一条消息到底有没有进记录，直接看这个文件最后几行最靠谱。

---

## 七、本次改动速览

- index.js 中，发送到酒馆、新增的 injectMesBubble、收藏注入到聊天这几处入口，全部改成先 push 进记录、再增量插气泡上屏。原来会崩界面的 reloadCurrentChat 实际调用已清零。
- 最终三处文件内容一致。
- 做了多份备份用于回滚。

---

本文档会随后续开发持续补充。发现新坑就按日期追加一节，避免重蹈覆辙。
