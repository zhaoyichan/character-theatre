# 小剧场（character-theatre）· 开发介绍

> 面向开发者/后续维护者的技术文档。从「这插件是怎么搭起来的」讲起，覆盖架构、设计取舍、各功能模块实现要点、以及如何安全地继续改。
> 想快速上手改代码，先读本文件 + `开发日志.md` + `开发经历与问题处置全记录.md`。

---

## 一、技术栈与形态

- **形态**：SillyTavern 独立第三方插件，自给自足，不依赖任何宿主插件，纯前端（JS + CSS）。
- **实现**：单文件 `index.js`（约 2027 行，一个 IIFE 包裹）+ `style.css`（浅色皮肤）+ `manifest.json`（酒馆注册）。
- **运行环境**：Android 上酒馆的 WebView。
- **存储**：localStorage（键见下），外加内存权威缓存 `_cache` 防抖动。

### 关键约束（受 WebView 限制）
1. **不能用 `window.prompt/confirm/alert`** → 全部自绘弹窗。
2. **Android data 目录 terminal 读不了** → 写要走 `super_admin:shell`。
3. **改完要完全冷启动酒馆**才加载新扩展。

---

## 二、目录与三处同步

| 用途 | 路径 |
|---|---|
| 运行目录（真身） | `/storage/emulated/0/Android/data/com.tauritavern.client/data/extensions/third-party/character-theatre/` |
| 开发镜像 | `/storage/emulated/0/Download/酒馆插件开发/酒馆小剧场开发/character-theatre/` |
| 需求铁律 | `/storage/emulated/0/Download/酒馆插件开发/liwe记忆/需求-小剧场功能.md` |

改完必须两边 `cp` 同步 + 冷启动。

---

## 三、架构 / 代码组织（照 `index.js` 顺序）

`index.js` 一个 IIFE，大致分块：
1. **数据层**：`safeGet/safeSet`（内存缓存 + localStorage 双写）、`getGroups/saveGroups`、`getGlobalMap/getChatMap`、`grabText/applyReplace/resolveNames`。
2. **UI 基础设施**：`ensureRoot/getPanel/reg/runH/ico/topBar/toast`、自绘弹窗 `thModal/thPrompt/thConfirm/thSquare/thPrompt`、底部诊断条 `logEvent/renderDebugBar`。
3. **三屏渲染**：`renderHome`（分组列表）、`renderGroupView`（组内条目）、`renderItemDetail`（详情编辑）。
4. **设置界面**：`renderSettingsView`（替换名称/作用范围/分组管理/备份恢复）。
5. **收录**：双击消息 + 每条右下角 ✓ 收录角标 → `openCollectDialog` → 反替换收进组。
6. **收藏**：`favEnsureBtns/scanMesForFav/favCollectMes/grabMesHtml/cleanFavHtml`（收藏、穿透 shadow-root 抓图、引号清洗）+ `favOpenView/favRenderView/favBindView`（查看界面）+ `favInjectToChat/favConfirmTop/favExportHTML/favExportZip/favImportZip`。
7. **单条快照**：`mesCornerBtn` 注入角标、`thCopyOne`（复制这条）、`thCaptureOne`（拍照这条→PNG）。
8. **入口**：`registerEntry`（可拖动悬浮球 + 位置记忆）。

### 三处数据存储键
- `th-theatre-data`：分组/条目
- `th-map-global`：全局替换组；聊天记录替换组挂 `ctx.chat.__theatreChatMap`
- `th-fav-store`：收藏库
- `th-fabpos`：悬浮球位置

---

## 四、核心设计取舍（为什么这么做）

1. **自绘弹窗 + 顶层挂载**：WebView 禁原生弹窗；且凡是浮在收藏窗(999990)/聊天之上的确认，都要挂 `document.body` + z-index 999999+，否则被遮罩挡住点不到（本次「注入/删除没用」就是栽在这）。
2. **收藏抓图穿透 shadow-root**：柏宝绘成品图渲染在自定义元素 shadowRoot 里，`innerHTML` 抓不到 → `grabMesHtml` 通过 `data-bbi-slot` 锚点进 shadowRoot 抓 `<img>` 原位插回。
3. **引号清洗两步**：`<q>` 会浏览器自动加引号 → CSS `quotes:none` 禁掉 + `cleanFavHtml` 单节点/相邻节点两步去重复引号。
4. **拍照双保险**：安卓 WebView 里 `foreignObject` 不一定能画进 canvas → 方案A（foreignObject）+ 方案B（纯文本竖屏 canvas 兜底），保证 PNG 一定产出且竖屏显示全部文字。
5. **悬浮球可拖动**：`touch-action:none` 防滚动抢拖动；位移 >8px 判拖动（不误开）；localStorage 记忆位置。
6. **内存权威缓存 `_cache`**：读写都先过它，避免 WebView localStorage 抖动丢数据。
7. **`saveGroups` 只收数组**：防误传单个组对象污染整库。

---

## 五、诊断与验证姿势

- **改完验证**：`node --check index.js`（先 cp 到 /sdcard/Download 再跑），再冷启动酒馆。
- **诊断日志**：小剧场面板底部有一条可展开的诊断条（`toggleDebugBar`），显示内存 groups / localStorage 原始 / 最近所有 `logEvent` 动作（如 `注入-*`、`拍照-*`）。真机排查先把这些日志读给开发者/记进交接。
- **大改动流程**：temp 文件 + Python 按行 splice 或 shell 直写 → cp 真身 → node --check，别用沙箱 edit_file 反复拼。

---

## 六、给后续维护者的扩展指引

- **加新一组设置项**：在 `renderSettingsView` 加卡片，数据走现有 `getGroups/saveGroups` 或新增 localStorage 键，别直接改状态机入口。
- **加新收藏批量操作**：仿照 `favInjectToChat`（顶栏加按钮 + `favBindView` 加分支 + 顶层确认），复用 `favConfirmTop` 做确认，别用 `thConfirm`。
- **加新弹窗**：用 `thModal`（面板内）或挂 body 的顶层（`favPrompt/favConfirmTop` 模式），视要盖在哪层而定。
- **图标**：统一在 `ICO` 对象里加 SVG path，用 `ico(name, size)` 取；别新增 emoji。

---

## 七、发布到 GitHub

本地 `git` 协议端可能被网络限制（`github.com` git 端口超时），用 **GitHub REST API** 提交：
读 HEAD → 逐个 `POST /git/blobs`（base64，用 `-d @file` 避免参数过长）→ `POST /git/trees` → `POST /git/commits` → `PATCH /git/refs/heads/main`。
仓库：`zhaoyichan/character-theatre`（main），作者账号 `zhaoyichan` / 姓名 `liwe`。

---

*本文件随架构演进同步更新；与开发日志、问题处置记录互补。*
