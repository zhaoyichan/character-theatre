# 小剧场插件 · 开发日志与交接（character-theatre · v2.0.0）

> 本文件是**唯一权威开发/交接底稿**。任何人接手、改代码，必须：
> 1. 先读完本文件；
> 2. 每完成一轮改动，在文首「更新目录」追加记录（日期 + 做了啥 + 改了哪些文件/函数 + 如何验证）。
>
> 优先级高于对话记忆。所有「血泪坑」都记在下方，**禁止重复踩**。

---

## + 更新目录（最新在最上）
- **[2026-09-09] 收藏存储升级 IndexedDB（大容量）+ 自动迁移老数据 + 双层兜底**：主人反馈收藏到 52 条就再也收藏不上，且长按能弹标题、显示已收藏，但查看界面条数不增加、无任何报错。根因分两层：① 早期收藏存 localStorage 仅 ~5MB，存几十条含图收藏即爆容量（同《存档功能-存储方式详解.md》历史教训）；② favLoad 只认 localStorage 旧数据、挡住扩展设置里已写入的新数据。已在上一轮修复②（双通道融合）。本轮根治①：仿角色日记存档系统(cd-save-db)把收藏迁到 IndexedDB（库 th-fav-db / store lib / key main，GB 级）。改动：① 新增 favIDBDb/favIDBGet/favIDBSet 读写 IndexedDB；② 新增 __favCache 会话内权威快照，favLoad 优先读缓存（避免异步卡 UI），缓存未就绪才回退同步读 localStorage/扩展设置；③ favPersist 同步回填缓存+写 localStorage/扩展设置兜底后，write-through 异步写 IndexedDB 并记日志；④ 新增 favLegacyPick/favBootstrap，registerEntry 启动时自动把老收藏(52条)迁入 IndexedDB 一分不少。验证：node --check 通过；Node 逻辑级验证 4 场景全 PASS；开发副本+运行目录 md5 一致 636ce930（2628 行），需完全冷启动酒馆生效（冷启动时自动触发迁移）。
- **[2026-09-09] 修复：收藏「加了却看不到」（双通道读取被旧数据挡住）**：主人反馈长按收藏消息能弹标题、toast 显示已收藏，但查看界面条数一直停在 52 条不增加且无报错。根因：收藏写入是双通道（localStorage + 酒馆 extension_settings），但 favLoad() 只认 localStorage——只要 localStorage 还能读到（哪怕是旧 52 条）就永远不读扩展设置那份新数据；当 localStorage 写失败、数据只成功落在扩展设置时又只在"两路全失败"才提示，导致"收藏成功但界面读旧数据→永远看不到新增且无告警"。修复：① favLoad 双通道融合——localStorage 与扩展设置都读，取「条数更多/最新收藏 time 更新」的一份，不再被旧数据堵死；② favPersist 返回 {lsOk,extOk}，落盘结果显式透出；③ favCollectMes 按落盘结果区分提示：localStorage 成功=「已收藏·角色」、仅扩展设置=「已收藏(仅备份通道)·请尽快全量备份」、两路全失败=「收藏保存失败，未落盘」，不再假装成功。改动：favLoad / favPersist / favCollectMes。开发副本+运行目录 md5 一致 5b54fde7（2529 行），node --check 通过，需冷启动酒馆生效。（不新增条数上限；已确认代码无 52 条硬限制。）
- **[2026-09-09] 修复：导入ZIP内容含PK误截断 + 全量恢复兼容两种备份格式**：① favImportZip 原用「找 data.json 后再找下一个 PK 截断」的暴力解析，收藏内容里一旦出现 ASCII「PK」会被拦腰截断导致导入失败（Node 实测复现）。改为新增 _thExtractZipEntry 按 local file header 精确逐条目定位（读 nameLen/extraLen/csize 计算真实 data 偏移并跳过条目内容），彻底杜绝内容含 PK 误截断——实测含 PK 收藏可完整解析。② thFullRestore 原只认 thFullBackup 双层格式，老 exportAll 平铺备份读不进；改为兼容三种：平铺(data.groups)/双层(data.data.groups)/裸数组。此前两处备份格式割裂问题解决。改动：新增 _thExtractZipEntry，改 favImportZip、thFullRestore。开发副本+运行目录 md5 一致 853e9308（2508 行），node --check 通过，需冷启动酒馆生效。备份：index.js.bak_zip解析修复_20260908。
- **[2026-09-09] 收尾：导入ZIP改追加 + 收藏写失败可见 + 备份含收藏 + 更正误报**：① favImportZip（导入ZIP到收藏查看器）从覆盖改为追加——读现有 favLoad()，把 zip 里 items 全部 concat 进现有（无视命名重复、不覆盖），导入前 thSnap(快照) 兜底，提示现有共多少条；② favPersist 的 localStorage 写失败不再静默，双通道都失败 toast 提示+日志；③ exportAll（老导出备份）补上 fav 收藏字段，三块齐全；④ 清理残留多层空行。更正：上一轮报告中「importAll 的 skipCnt 忘了累加」经复核实际已累加，非 bug，未改动。改动：favImportZip / favPersist / exportAll。开发副本+运行目录 md5 一致 df9d609d（2495 行），node --check 通过，需冷启动酒馆生效。备份：index.js.bak_全改_20260908。
- **[2026-09-09] 修复：全局加固卡被钉在设置页底部（改回随滚动列表）**：主人反馈：设置页本该整页上下滑动，但全局加固卡被固定在底部一栏不随页面滚。根因：之前用「包裹 renderSettingsView 在返回后追加」的方式插入，卡片被拼到了 th-list 滚动容器闭标签之外、panel 底部，成为固定底栏且无法随列表滚动。修复：① 把 thSettingsExt() 调用直接写进 renderSettingsView 函数体内、th-list 容器收尾 </div></div> 之前——现已作为普通卡片与其它设置卡并列、随页面上下滚；② 删除外层包裹覆盖段 _thOriRenderSettings（避免双份：一份在列表内+一份钉外面）。改动：renderSettingsView 收尾 + 删包裹段。开发副本+运行目录 md5 一致 bfc5593c（2483 行），node --check 通过，需冷启动酒馆生效。
- **[2026-09-09] 修复：退出酒馆重进条目/组丢失（三通道可靠落盘 + 双通道读取回退）**：主人反馈［明明多个条目/组，退出重进只剩一两个，内容大量缺失］。根因：原数据层只有 localStorage 单通道，且 rawSet 写失败被空 catch 静默吞掉；内存 _cache 是权威缓存但进程一杀就归零，重进从磁盘读到的是更早更少的旧版本，导致最后写入的条目/组丢失。修复（纯数据层替换，调用方零改动）：① rawSet 写失败不再静默，返回是否落盘成功，失败进诊断日志；② 新增 thReliableSet 统一可靠落盘：localStorage + 酒馆 extension_settings 双通道（收藏线早有这层所以更稳，现把分组/替换组同步接入）；③ safeSet 经 thReliableSet 双通道写，双通道均失败时 toast 提示可能丢失；④ rawGet/favLoad 增强双通道读取回退：localStorage 为空自动回退读酒馆扩展设置，某通道丢另一通道兜底；⑤ favPersist 原本双写，现 favLoad 也能从扩展设置读回。同时把设置页全局加固卡改为 details 折叠紧凑版（默认收起不占屏，对齐现有 th-det 折叠范式）。改动：index.js 数据层 + thSettingsExt。开发副本+运行目录 md5 一致 866e96b2（2489 行），node --check 通过，需冷启动酒馆生效。备份：index.js.bak_保存逻辑修复_20260908。
- **[2026-09-09] 新增：全局加固——小剧场/收藏存得死死的（冷备份 + 自动快照 + 持久化校验 + 写入口自动沉淀）**：主人反馈"存储走什么线路、容易丢东西"，已确认三块全局数据（小剧场分组 th-theatre-data / 全局替换组 th-map-global / 收藏 th-fav-store）全部保持跨角色全局，不随角色/聊天走。本次纯增量追加加固模块（不改动任何既有逻辑），落点 = index.js IIFE 尾部。功能：① thFullBackup/thFullRestore 全量冷备份/恢复（三块全局数据一次导出为独立 JSON 文件落盘到 Download，清缓存也不丢）；② thSnap/thRestoreSnap 自动快照（存独立键 th-snapshots 保留最近 5 份、去抖 1200ms，可一键回滚）；③ thVerifyPersistence 校验落盘（回读 localStorage 与内存权威缓存 _cache 比对，暴露假保存成功）；④ 写入口自动快照（saveGroups/saveGlobalMap/favPersist 每次真实写盘后 3 秒节流自动沉淀一份快照）；⑤ 离页/切后台 pagehide/visibilitychange 最后一拍快照。设置页新增"全局加固"卡片：全量备份/全量恢复/存快照/恢复快照/校验落盘 五按钮（沿用现有 ICO 键内联 SVG，零 emoji）。改动：index.js 追加约185行。开发副本+运行目录 md5 一致 ccc37d02，node --check 通过，需冷启动酒馆生效。备份：真身 index.js.bak_全局加固_20260908_210549。
- **[2026-09-08] 修复：「发送到酒馆」时灵时不灵 + 聊天界面崩成1条（根治 · 增量插气泡方案定稿）**：定位根因——消息其实每次都成功 push+saveChat 落盘（jsonl 验证），但原 sendText 第3步调 `reloadCurrentChat()` 会**整段清空重绘聊天界面**，重绘遇到特定内容（`$` 开头/超长文本）时渲染异常，界面崩成只剩1条甚至带 `${characterName}` 宏占位符；且这个定制客户端里 `MESSAGE_SENT` 事件**不会**把新用户消息渲染成气泡（只入列不渲染）。**最终方案（v3，已下沉为通用）**：新增 `injectMesBubble(content,userName)`，**克隆 `#chat` 里最后一条 `.mes` 气泡 → 把文本节点换成发送内容 → 插到 `#chat` 末尾 → 滚到底**（只增量加 1 气泡，不整段重绘）。`sendText` = push+saveChat+`injectMesBubble`+emit（保留 `发送-*` 诊断日志）。**收藏注入 `favInjectToChat` 同步改成**：push 全部选中收藏 + saveChat + 逐条 `injectMesBubble` 上屏 + emit，**全文件 `reloadCurrentChat()` 实际调用已清零**（仅注释提及）。改动：sendText(721起)、injectMesBubble(新)、favInjectToChat(1776起)。同步三处 md5 一致 `dacff292`，node --check 通过。备份真身：`index.js.bak_收藏注入也改增量_20260908`、`index.js.bak_发送修复v2.5_20260908_2100`。**血泪教训详见《开发经验与避坑记录-20260908发送修复专档.md》。**
- **[2026-09-07] 新增：收藏查看界面按收藏时间降序（最新在前）**：favRenderView() 原样读 store.items、顺序=收藏先后（旧的在上）。改为拷贝后 .sort((a,b)=>(b.time||0)-(a.time||0)) 按 time 降序。分组后每组/各组均最新在前。改动：favRenderView 第1412行。已同步开发副本+运行目录（md5 65fd97c4），node --check 通过，需冷启动酒馆生效。
- **[2026-09-07] 修复：编辑条目返回后回到原滚动位置**：组列表往下滑点进条目编辑，render() 用 innerHTML 整屏重建导致返回列表时 scrollTop 归零、回到顶部。新增 _scrollMem 记忆 + SaveScroll/RestoreScroll：openItem() 进详情前记下 .th-body.scrollTop，render() 在 group 视图末尾还原（用完即清）。改动：openItem/render。已同步开发副本+运行目录（md5 dcdb604e），node --check 通过，需冷启动酒馆生效。
- **[2026-09-07] 修复：导入小剧场由「整体覆盖」改「追加合并+跳过重复」**：原 `importAll()` 在 783-785 行直接 `saveGroups(g)` 把导入文件整体替换 `th-theatre-data`，导致主人已有小剧场被冲掉。已改为：先读现有 `getGroups()`，与导入文件逐组合并——同名组条目追加到末尾、新组名并入末尾；**跳过重复**（同组内 title 且 content 均相同者不追加）；**不做**整体替换选项。改动函数 `importAll()`；已同步开发副本+运行目录（两处 md5 一致 `96d527bd`），开发副本 `node --check` 通过，需冷启动酒馆生效。备份：`index.js.bak_覆盖导入修复_20260907`（两处各自都有）。（注：合并逻辑同时收敛导入条目的 title/content，重复判定 title 且 content 都相同）

- **[2026-09-04] v2.0.0 版本发布 + 上传 GitHub**：manifest.json 版本 1.0.0→2.0.0，author 修正为 liwe和他的后宫老婆们，description 全面扩写；新增 README.md（介绍界面，零 emoji）；本开发日志重写为全量交接。
- **[2026-09-04] 悬浮球可拖动**：registerEntry 改造为可长按拖动，touch-action:none，位移>8px 判定拖动（不误开面板），localStorage('th-fabpos') 记忆位置，clamp 边界 + resize 自适应。
- **[2026-09-04] 收藏多选「删除」失效修复**：删除确认 thConfirm → favConfirmTop（顶层 fixed + z-index:9999999 挂 body）。
- **[2026-09-04] 收藏多选「注入到聊天」修复**：确认框 thConfirm → 顶层弹窗，新增 favInjectToChat（一次性 push 全部选中收藏为发言）+ 全套注入-* 日志。
- **[2026-09-04] 拍照 PNG 修复 + 竖屏全文字**：thCaptureOne 重写，foreignObject→canvas 优先、纯文本竖屏 canvas 兜底（measureText 自动换行显示全部），加 拍照-* 日志。
- **[2026-09-04] 收藏界面皮肤 → 小剧场风**：style.css 的 #th-favroot 全套配色/圆角/顶栏改为走小剧场 CSS 变量（米白/深藏青/金/16px/50px顶栏）。
- **[2026-09-04] 拍照/复制改为注入每条消息右下角**：从屏幕右下角与悬浮球并列，改为每消息右下角与 ★收藏/✓收录 同排（复制 right:74 / 拍照 right:98）。
- **[2026-09-04] 修复 edit 工具导致的文件尾重复损坏**：改用 super_admin:shell 直改真身 + node --check 全程守门，杜绝沙箱 desync。

---

## 一、这是什么

独立 SillyTavern 第三方插件「小剧场」：
- 按分组存可复用对白/桥段；发送时替换 {{user}}/{{char}} 后进聊天。
- 收藏聊天记录，按角色分组查看、多选删除/导出/注入、ZIP/HTML 备份。
- 每条消息右下角拍照(PNG)/复制；可拖动悬浮球。
- 自给自足，不依赖宿主插件。

插点名：character-theatre；显示名：小剧场；作者：liwe和他的后宫老婆们；版本：2.0.0。

---

## 二、文件位置（三处必须同步）

| 用途 | 路径 |
|---|---|
| 运行目录（生效，需 root 写） | /storage/emulated/0/Android/data/com.tauritavern.client/data/extensions/third-party/character-theatre/ |
| 开发副本（改这＋同步回去） | /storage/emulated/0/Download/酒馆插件开发/酒馆小剧场开发/character-theatre/ |
| 需求铁律（改前先读） | /storage/emulated/0/Download/酒馆插件开发/liwe记忆/需求-小剧场功能.md |

- 改完必须完全冷启动酒馆才生效（只刷新不行）。
- Android data 目录普通 terminal 读不了（Permission denied）→ 必须 super_admin:shell cp；node --check 须先 cp 到 /sdcard/Download/ 再跑。

---

## 三、已实现功能（全量）

### 小剧场本体
- 分组/条目/收藏；标题+正文；{{user}}/{{char}} 宏替换（三形态）
- 全局 + 聊天记录 两套替换组，可存成组、自由切换、自定义「＋新增替换」
- 发送（进聊天气泡，不触发生成）/ 填框（只进输入框）
- 详情编辑；导入导出 JSON；外部 miniStage 转换

### 收藏聊天记录
- 长按 / 点 ★ 收藏（写标题小窗 favPrompt 顶层）
- grabMesHtml 穿透 shadow-root 抓「柏宝绘」成品图原位插回
- 收藏界面按角色分组折叠 + 筛选胶囊 + 阅读页（details 折叠、引号清洗）
- 多选：删除 / 导出HTML / 注入到聊天（favInjectToChat）
- 设置页：导出ZIP / 导入ZIP / JSON备份

### 单条消息快照
- 每条消息右下角：✓收录(2) ★收藏(50) 复制(74) 拍照(98)
- thCopyOne 复制这条；thCaptureOne 拍照这条 → PNG（竖屏全文 + 兜底）

### 可拖动悬浮球
- 长按拖动 + touch-action:none + 拖动≠点击 + 记忆位置 th-fabpos

### 界面
- 全 SVG 零 emoji，单列紧凑，浅色护眼；收藏界面走小剧场皮肤变量

---

## 四、数据键（localStorage）

- th-theatre-data：分组/条目 [{name,items:[{title,content,fav}]}]
- th-map-global：全局替换组 {groups:[...],current}
- chat 上字段 __theatreChatMap：聊天记录替换组
- th-fav-store：收藏 {v:1,items:[{id,role,chatTitle,startFloor,endFloor,time,note,title,msgs:[...]}]}
- th-fabpos：悬浮球位置 {x,y}
- 内存权威缓存 _cache：读写先过它，防抖动

---

## 五、关键函数速查（照名搜）

数据层：getGroups/saveGroups/findGroup/safeItems/getGlobalMap/getChatMap/grabText/applyReplace/resolveNames
收藏：favLoad/favPersist/favOpenView/favCloseView/favRenderView/favBindView/favCollectMes/favPrompt/favConfirmTop/grabMesHtml/cleanFavHtml/bindMesLongPress/favEnsureBtns/scanMesForFav/favInjectToChat/favExportHTML/favExportZip/favImportZip
单条快照：thCopyOne/thCaptureOne/iconCam/iconCopy
入口：registerEntry/mesCornerBtn
诊断：logEvent/renderDebugBar/toggleDebugBar

---

## 六、踩过的坑与解法（血泪，务必记）

1. 安卓 WebView 禁 prompt/confirm → 一律自绘弹窗。
2. 弹窗挂 getPanel() 在聊天/收藏界被遮罩盖住：
   - 坑4/5：收藏标题窗、双击收录窗、顶层确认窗（删除/注入，favConfirmTop）必须挂 document.body + 顶层 z-index。
   - 这是本次「注入没用」「删除没用」的共同根因：thModal(z:50 挂 panel) 被 #th-favroot(z:999990) 遮罩压在底下。
3. 数据污染：saveGroups 只收数组，改动存整组。
4. gs.reduce is not a function：getGroups() 强制 Array.isArray 归一化。
5. 双击收录失效 → 原生 addEventListener，不用 jQuery .on。
6. 收藏图片丢失：shadow-root 里的柏宝绘成品图 → grabMesHtml 穿透 data-bbi-slot shadowRoot 抓 img 原位插回。
7. 对话双引号变两对：<q> 自动加引号 → CSS 禁语义引号 q{quotes:none} + cleanFavHtml 两步收敛。
8. 思维链折叠点不开：summary::-webkit-details-marker{display:none} + ::after 箭头。
9. zip 手写（crc32 + 目录 + EOCD）已验证可解压。
10. 拍照 SVG 相册看不了 → 改 PNG；「保存失败」根因 = foreignObject 栅格化进 canvas 在安卓 WebView 常失败 → 加纯文本竖屏 canvas 兜底，保证一定出 PNG。
11. 运行目录 terminal 写不了 → 必须 super_admin:shell。
12. 重要：沙箱 edit 工具会 desync/损坏文件尾（本会话踩过，把 1869 行文件尾搞出重复乱码）。改大文件改用 super_admin:shell 直改 + node --check 守门，不要依赖沙箱 edit_file。

---

## 七、待办 / 下一位重点

1. 收藏界面皮肤已对齐小剧场，但 favChromelike/收藏查看与收藏界面尺寸、皮肤统一仍需最终核对。
2. 冷启动真机验证：注入、删除、拍照 PNG 竖屏全文、悬浮球拖动，均真机确认最终效果。
3. 诊断日志利用：真机排查时，看小剧场底部诊断条注入-*/拍照-* 日志定位。

---

## 八、如何验证 / 发布

- 每次改：node --check（先 cp 到 /sdcard/Download）+ 冷启动酒馆。
- 改完同步三处（开发 / 运行）。
- 大改动前先 cp index.js index.js.bak_原因。
- 发布到 GitHub：git add . && git commit && git push（用主人 repo + token）。

---

*本文件随新进展同步更新；优先级高于对话记忆。*
