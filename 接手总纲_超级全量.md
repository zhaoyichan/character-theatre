# 小剧场（character-theatre）· 超级接手总纲（AI 专用 · 全量 · 2026-09-05）

> 这不是普通的交接文件，是给「下一个接手 AI/开发者」的**全量操作手册**。
> 目标：你只要读完这一份，就能独立完成任何我做过/可能被要求做的事——知道文件在哪、改哪、哪些不能碰、出错在哪查、仓库怎么传。
> **主人是小白，不会给你流程；你必须自己从这份手册里推导出完整流程。**

---

## 〇、这篇文档怎么用（先读这段）

- 遇到任何需求，**先看「一、文件在哪」**确定动哪个文件；
- **再看「三、改代码铁律」**确定怎么改（千万别踩）；
- **改完看「二、部署三处同步 + 验证」**，同步 + node --check + 冷启动；
- **要上 GitHub 看「八、仓库上传」**。
- 这份文档**覆盖本插件全部现状**（截至 2026-09-05，v2.0，index.js 2068 行）。

---

## 一、文件在哪（最重要，先记死）

这个插件叫「小剧场」，英文 `character-theatre`，作者 `liwe和他的后宫老婆们`，当前版本 v2.0.0。

### 三个目录（必须分清，别搞混！）

| 用途 | 绝对路径 | 谁能写 | 作用 |
|---|---|---|---|
| **A. 酒馆运行目录（真身）** | `/storage/emulated/0/Android/data/com.tauritavern.client/data/extensions/third-party/character-theatre/` | 仅 root（`super_admin:shell`） | 酒馆实际加载的插件文件。**改这里才生效** |
| **B. 开发镜像（工作副本）** | `/storage/emulated/0/Download/酒馆插件开发/酒馆小剧场开发/character-theatre/` | `super_admin:terminal` 或 `shell` | 你编辑/查看用这兆。改完同步回 A |
| **C. GitHub 仓库** | `zhaoyichan/character-theatre`（main 分支） | REST API + token | 发布给别人。含全部文档 |

> **关键坑**：
> - **Python/终端脚本都写到 `/storage/emulated/0/Download/` 下**，不要直接写到 A（会 Permission denied）。A 只能用 `super_admin:shell`（root）`cp` 写入。
> - A 和 B 的 `index.js` / `style.css` / `manifest.json` 必须**内容一致**（一般都 from B cp 到 A）。
> - 改完 A 必须**完全冷启动酒馆**（force-stop + 重开，不是刷新）才加载新扩展。

### 插件内每个文件是干嘛的

| 文件 | 大小(约) | 作用 | 改不改成 |
|---|---|---|---|
| `index.js` | 124 KB · 2068 行 | **全部逻辑**（UI/数据/行为/收藏/导出/拖拽） | 核心，几乎都改这里 |
| `style.css` | 24 KB · 262 行 | 全部样式（浅色皮肤、SVG、无 emoji） | 改外观时 |
| `manifest.json` | 521 B | 插件注册（name/id/version/author/js/css） | 改版本/作者时 |
| `README.md` | 3.16 KB | 社区介绍（功能清单式） | 发布介绍时 |
| `开发日志.md` | 7.7 KB | 迭代记录 | 每次改完要更新 |
| `开发介绍.md` | 5.9 KB | 面向开发者的架构介绍 | 极少数改 |
| `开发经历与问题处置全记录.md` | 10 KB | 踩坑详细记录 | 有新坑就追加 |
| `交接总纲-此刻全量.md` | 9.4 KB | 旧版交接 | 已过时，以本文件为准 |
| `小剧场-交接文档.md` | 4.8 KB | 旧版交接 | 已过时 |
| `本文件` | 超长 | 超级接手总纲 | 持续维护 |

---

## 二、快速上手：改一行要几步

**任何需求的标准流程（背下来）：**

1. **定位**：按下面的「三、想干嘛去哪改」找到对应函数/文件。
2. **改 B（开发镜像）**：直接用 `super_admin:terminal` 的 `node --check` 验证语法前，先把 index.js 用工具读出精确定位。
   - **大段逻辑写进 `/storage/emulated/0/Download/_xxx.js`（temp），再用 Python 按行/锚点 splice 进 B 的 index.js**（别用沙箱 edit_file！会毁文件）。
3. **验证 B**：`node --check '/storage/emulated/0/Download/酒馆插件开发/酒馆小剧场开发/character-theatre/index.js'` 必须输出无错误。
4. **同步到 A**：`cp B/index.js A/` 等三个文件，用 `super_admin:shell`（root）。
5. **验证 A**：`cp A/index.js /storage/emulated/0/Download/_v.js && node --check /storage/emulated/0/Download/_v.js`。
6. **通知主人冷启动**酒馆，看效果。
7. **更新 `开发日志.md`**（文首「更新目录」追加一条），有新坑就追加到问题记录。

---

## 三、想干嘛 → 去哪改（核心地图）

以下每条 = 主人可能的需求，直接告诉你去哪找。**函数名 ← `index.js` 里的名字，用 /system/bin/grep / 搜索定位行号。**

### 3.1 界面尺寸/布局
- **面板宽度高度**：`ensureRoot()` 里的 `panel.style.cssText`（约 index.js 127 行），当前 `width:480px;max-width:94%;height:min(500px,92%)`。
- **收藏查看窗尺寸**：`favRenderView()` 里 `winEl.style.cssText`（约 1398 行），当前同样是 `480×min(500,92%)`。两处必须同步。
- **卡片/圆角/配色**：全部在 `style.css`。CSS 变量定义在文件顶部 `:root{}`。
- **顶栏高度/顶栏文字**：`style.css` 里的 `.th-top` / `.th-fav-top`。

### 3.2 聊天消息右下角角标（★收藏 / ✓收录 / 拍照 / 复制）
- 注入位置：`favEnsureBtns(mes)`（每条消息右下角放 ★收藏±复制±拍照）、`collectAddIcon(mes)`（放 ✓收录）。
- 角标坐标：在函数里的 `right:xx px`（✓=2 ★=50 复制=74 拍照=98）。
- 通用角标生成：`mesCornerBtn(mes, opts)`。

### 3.3 收藏功能
- **收藏一条（长按/★）**：`favCollectMes(mes)`。长按判定在 `bindMesLongPress(mes)`。
- **长按误触（AI 生成时点屏弹窗）**：已修，在 `bindMesLongPress` + 模块级 `__longPress/__cancelLongPress` + `__wireLongPressGlobal`（document 级 touchend 兜底 + scroll 抑制）。**这是最易回退的点，别删。**
- **收藏弹窗（写标题）**：`favPrompt`。
- **收藏界面打开/渲染/事件**：`favOpenView/favRenderView/favBindView`。
- **收藏筛选胶囊**：**已删除**（主人要求去掉顶部角色胶囊）。`favRenderView` 里不再有 `fav-filter`。残留 `data-favflt` 事件是死代码，无害，但主人明确不要胶囊，别加回去。
- **收藏多选删除/导出/注入**：在 `favBindView` 的事件分发（`data-favdel` / `data-favexport` / `data-favinject`），确认弹窗用 `favConfirmTop`。

### 3.4 设置页
- 在 `renderSettingsView`。含：替换名称 / 作用范围 / 分组管理 / 备份恢复。
- **「保存当前组」**：`saveCurrentGroup`（取替换名称卡内容+弹框输标题+同名覆盖+自动选中）。
- **「建立新组」**：`buildNewGroup`（弹框手动输标题+你名+角色名）。
- **草稿状态**：`window.__thDraft`（没选组时暂存输入，防止退出丢失）。

### 3.5 单条消息拍照/复制
- `thCopyOne(mes)` 复制这条；`thCaptureOne(mes)` 拍照这条→PNG。
- 拍照有双保险：方案A foreignObject→canvas，方案B 纯文本竖屏 canvas（`textFallback`）。别删 B。
- 图片/图标：`iconCam/iconCopy`。

### 3.6 悬浮球
- `registerEntry()`（可拖动，`touch-action:none`，locaStorage('th-fabpos') 记忆位置，clamp 边界）。
- 拖动手势在它内部的 `place/clamp/persist` 等。

### 3.7 宏替换 / 数据
- 数据层：`getGroups/saveGroups/findGroup/safeItems` 等，localStorage 键见下。
- 宏替换：`grabText/applyReplace/resolveNames`。
- 替换组：全局 `th-map-global`，聊天 `__theatreChatMap`。

### 3.8 导入导出 / 备份
- `exportAll`(JSON) / `importAll` / `resetAll` / `favExportHTML` / `favExportZip` / `favImportZip`。
- ZIP 工具：`buildZipAndDownload/crc32/concat/localTime`（手写 ZIP，STORE 无压缩）。
- **「一切皆可收纳」插件导出的 ZIP 也能导入**（格式 `{v,items}` 兼容）。

### 3.9 诊断日志
- `logEvent/renderDebugBar/toggleDebugBar`。底部诊断条可展开看 `注入-*`/`拍照-*` 日志。真机排查第一件事：**让主人打开小剧场底部诊断条，把日志告诉主人复述给你**。

---
---

## 四、数据键（localStorage + 内存）- 这些是插件的"血库"

| 键名 | 存什么 | 结构 |
|---|---|---|
| `th-theatre-data` | 小剧场分组/条目 | `[{name, items:[{title, content, fav}]}]` |
| `th-map-global` | 全局替换组 | `{groups:[{name,user,char,extra?}], current}` |
| `__theatreChatMap` | 聊天记录替换组（挂 chat 对象上） | 同全局结构 |
| `th-fav-store` | 收藏聊天记录 | `{v:1, items:[{id,role,chatTitle,startFloor,endFloor,time,note,title,msgs:[...]}]}` |
| `th-fabpos` | 悬浮球位置 | `{x, y}` |
| `_cache`（内存） | 数据权威缓存 | 读写先过它，防 localStorage 抖动 |
| `window.__thDraft`（内存） | 设置页替换名称草稿 | `{user,char,extra}` |

**重点**：`saveGroups` 只接受数组（防止污染）；读数据走 `getGroups`（强制 Array.isArray 归一化）。这个千万别改成别的样子，会崩。

---

## 五、改代码铁律（"不能碰的点"，违反必出 bug）

1. **弹窗必须挂 `document.body` + 顶层高 z-index**。凡是要浮在收藏窗（z:999990）或聊天之上的确认/标题框，用 `favConfirmTop`（顶层 9999999）或 `favPrompt`，**绝不能用挂 `getPanel()` 的 `thModal`/`thConfirm`**——那会被收藏窗遮罩盖住，点了没反应（这是「注入没反应」「删除没反应」的历史根因）。
2. **大段代码别用沙箱 edit_file 拼**。它的 old/new 匹配会 desync、把文件尾搞出重复乱码（踩过，把 1869 行文件弄坏过）。**正确姿势**：写内容到 `/storage/emulated/0/Download/_xxx.js`，用 `super_admin:terminal` 跑 Python 按行/锚点 splice 进 B。
3. **index.js 改完必须 `node --check`**，且**同步 A（运行目录）+ 冷启动**。
4. **引号处理**：`style.css` 里 `q{quotes:none}` + JS `cleanFavHtml` 是配套的，别单独动其一。
5. **收藏抓图穿透 shadow-root**：`grabMesHtml` 通过 `data-bbi-slot` shadowRoot 抓柏宝绘成品图，别乱改，否则收藏图会丢。
6. **拍照双保险**：`thCaptureOne` 的 `textFallback`（纯文本竖屏兜底）必须保留，否则安卓 WebView 里 foreignObject 画不进 canvas 会报「保存失败」。
7. **长按防误触三件套**：`__longPress/__cancelLongPress/__wireLongPressGlobal` 是修「AI 生成时点屏弹收藏」用的，别删，否则那 bug 会回来。
8. **收藏界面顶部胶囊已被移除**：别加回 `favRenderView`。残留 `data-favflt` 死代码无害。
9. **按钮都在消息右下角同一排**：拍照/复制/★收藏/✓收录，主人要求同位置（right 2/50/74/98）。
10. **manifest 必须有 `author`**，否则酒馆拒载。发布记得升版本号。
11. **零 emoji + 全 SVG**：新图标/文案一律 SVG。

---

## 六、踩过的坑（症状对号入座）

1. 点按钮没反应（注入/删除）→ 弹窗挂错层级被遮罩盖。修：`favConfirmTop`。
2. 文件尾乱码 / node SyntaxError → 沙箱 edit_file 拼坏。修：temp+Python splice。
3. 收藏图丢 → `grabMesHtml` shadow-root 穿透被改。
4. 双引号两对 → `<q>` 自动加引号。修：CSS quotes:none + cleanFavHtml。
5. 拍照保存失败/横排截断 → foreignObject 画不进 canvas。修：textFallback。
6. AI 生成点屏弹收藏 → 长按 touchend 因重绘丢失。修：__wireLongPressGlobal。
7. 设置替换名称退出就没了 → 用 __thDraft + 保存当前组/建立新组。
8. 运行目录写不了 → 必须 shell(root) cp。
9. 改了不生效 → 没冷启动。
10. 面板尺寸 → 更宽更矮（480×500 统一），两处都要改。

---

## 七、真机排查标准流程

1. 让主人打开小剧场底部诊断条，把 `logEvent` 日志复述给你。
2. 看 `照片-成功/兜底`、`注入-完成/异常` 判断成功分支。
3. 读 B 的 index.js 对应函数。
4. 先定位根因（日志优先），不盲改。

---

## 八、上传 GitHub 完整流程

> 不用 git push（机器对 github.com git 端口不通），一律用 GitHub REST API。

**凭证**：Token `${GITHUB_TOKEN}`，账号 `zhaoyichan`/`liwe`。
**仓库**：小剧场 `zhaoyichan/character-theatre`；一切皆可收纳 `zhaoyichan/SillyTavern-Plugin-Harvest`。

**流程**（Python+curl，脚本写好放 Download 再跑）:
```
import json, base64, subprocess, tempfile
TOK='${GITHUB_TOKEN}'
API='https://api.github.com/repos/zhaoyichan/character-theatre'  # 换目标仓库

def api(method,url,body=None):
    cmd=['curl','-s','-m','40','-X',method,'-H','Authorization: token '+TOK,'-H','Accept: application/vnd.github+json','-H','User-Agent: ops']
    if body is not None:
        tmp=tempfile.NamedTemporaryFile('w',delete=False,dir='/root',suffix='.json'); tmp.write(json.dumps(body)); tmp.close()
        cmd+=['-H','Content-Type: application/json','-d','@'+tmp.name]
    cmd.append(url)
    r=subprocess.run(cmd,capture_output=True,text=True)
    try: return json.loads(r.stdout)
    except: return {'_err':r.stdout[:200]}

h=api('GET',API+'/git/ref/heads/main'); base=h['object']['sha']
entries=[]
for fn in <文件绝对路径列表>:
    name=fn.split('/')[-1]; data=open(fn,'rb').read()
    b=api('POST',API+'/git/blobs',{'content':base64.b64encode(data).decode(),'encoding':'base64'})
    entries.append({'path':name,'mode':'100644','type':'blob','sha':b['sha']})
t=api('POST',API+'/git/trees',{'tree':entries}); tree=t['sha']
c=api('POST',API+'/git/commits',{'message':'msg','tree':tree,'parents':[base]}); commit=c['sha']
api('PATCH',API+'/git/refs/heads/main',{'sha':commit,'force':True})
```
注意：全量覆盖最稳；中文文件名直接 UTF-8；传完 curl /contents 验证 size。

---

## 九、完整工作流（接到需求到交付）

1 听清需求(小白)→复述确认 2 定位文件 3 改B(temp+splice)+node--check 4 同步A(shell cp)+node--check 5 冷启动看效果 6 有问题→诊断条念日志排查 7 验收OK→更开发日志 8 要发布→REST API上GitHub 9 保持零emoji/三处一致/冷启动

---

## 十、主人真实意图

- 小白，描述常是感受/现象；要主动复述确认、给清晰操作路径。
- 信任 AI 搞定流程；但语义有歧义先对齐。
- 审美：零 emoji、全 SVG、紧凑小、单列、浅色护眼、收藏和小剧场同皮肤。
- 希望插件跨设备同步，大改/新功能要想到发布。
- 仓库 character-theatre 和 SillyTavern-Plugin-Harvest 都是主人的。

---

*本总纲覆盖 2026-09-05 现状，是最权威接手依据，优先于其它 md。*
---

## 十一、第二插件：「一切皆可收纳」（Everything Harvest）

> 这是主人的另一个插件，跟小剧场无关但常一起用。**它负责「把聊天记录收藏起来」；小剧场负责「台词素材库 + 二次收藏」。**
> 更重要的：**我们给 Harvest 加了「全部导出 ZIP」，能把它的收藏聊天记录导出，再导入小剧场。** 有跨插件需求时用这条链路。

### 11.1 位置
- 运行目录：`/storage/emulated/0/Android/data/com.tauritavern.client/data/extensions/third-party/SillyTavern-Plugin-Harvest/`
- 开发目录：`/storage/emulated/0/Download/酒馆插件开发/一切皆可收纳/`
- GitHub：`zhaoyichan/SillyTavern-Plugin-Harvest`（main）

### 11.2 它是干什么的
- 全局聊天收藏夹：长按/收纳，把聊天消息存进 `hv_store_v1`（结构 `{v:1, items:[...]}`，每条 `{id, role, chatTitle, startFloor, endFloor, time, note, group, msgs:[...]}`）
- 悬浮球收纳器、多选导出 HTML、移动分组、阅读页、引号清洗等。
- **我们加的功能**：顶部工具栏新增「全部导出ZIP」按钮（环形图标），把全部收藏打包成 zip（含 `data.json` + `index.html`），`data.json` 格式 `{v:1, items}`，**和小剧场 `th-fav-store` 同构 → 能导入小剧场**。

### 11.3 它加 的 zip 函数（在 Harvest 的 index.js）
- `hvBuildZip/hvZipCrc32/hvZipConcat/hvZipTime`（手写 zip 工具）
- `hvFavAllToZip()`（全部导出 zip）
- 图标 `IC.zip`、按钮 `data-a="exportzip"`、事件在 `bind()` 里 `a2==='exportzip'`（**必须放在 `if(act){...}` 内部**，否则点不触发——此点踩过）

### 11.4 Harvest 的改动方式
和改小剧场一样：temp写 /tmp 或 Download → Python splice → node --check → shell cp 到运行/开发 → 冷启动。注意 Harvest 用的是 `var` 风格旧代码，函数大多包在一个 `(function(){...})()` 里，别在外面引用它的内部函数。

### 11.5 跨插件工作流（主人可能会要）
```
[一切皆可收纳] 点环形图标 → 全部导出 ZIP
   ↓ zip 里 data.json = {v:1, items}
[小剧场] 设置 → 导入ZIP → 收藏进来（同构所以兼容）
```
如果主人问"怎么把A的收藏弄到B"，就是这条链路。

---

## 十二、index.js 完整函数索引（照名搜，不记行号）

> 任何功能，先在这里找函数名，再 `grep -n 'function 名字' index.js` 定位。

### 数据层
`safeGet safeSet rawGet rawSet getGroups saveGroups findGroup safeItems getGlobalMap saveGlobalMap getChatMap saveChatMap grabText applyReplace resolveNames evalText pickPicked`

### UI/渲染
`ensureRoot getPanel reg runH ico topBar topIcon renderHome renderGroupView renderItemDetail renderSettingsView chkLine groupCard groupCardSel empty toast`

### 行为
`open close goHome openGroup openItem goGroupBack newGroup newItem editItem delItem toggleFav sendText fillBox sendGrab fillGrab saveItemDetail sendFileDetail resetAll exportAll importAll saveGroup`

### 弹窗
`thModal thPrompt thConfirm thSquare favPrompt favConfirmTop`

### 收藏
`favLoad favPersist favEmpty favRoleName favChatName favRoleColor favCollectMes mesFloor grabMesHtml grabMesPlain cleanFavHtml bindMesLongPress favEnsureBtns scanMesForFav favOpenView favCloseView favRenderView favBindView favCreateTopIcon favInjectToChat favExportHTML favExportZip favImportZip favInlineImgs favMsgHtml`

### 收录（双击/✓）
`registerChatDblclick collectAddIcon openCollectDialog renderCollectDialog refreshColPreview collectApplyRules collectGetMesText`

### 长按防误触
`__longPress __cancelLongPress __wireLongPressGlobal tryChat`

### 单条快照
`thCopyOne thCaptureOne iconCam iconCopy savePng startRaster textFallback`

### ZIP 工具
`buildZipAndDownload crc32 concat localTime`

### 入口/悬浮球
`registerEntry mesCornerBtn place clamp persist`

### 设置分组
`saveCurrentGroup buildNewGroup`

### 诊断
`logEvent renderDebugBar toggleDebugBar`

### 多选
`toggleSelect selToggleGroup selToggleItem selAll selNone delSelectedGroups delSelectedItems`

---

## 十三、收藏数据 msgs 结构（关键，别搞错）

收藏的每条 `{id, role, chatTitle, startFloor, endFloor, time, note, title, msgs}`。其中 `msgs` 是消息数组，每个：
```
{ name, is_user, role, mes(纯文本), rendered(原HTML,含图), floor }
```
- `rendered` 是收藏时 `grabMesHtml` 抓的原样 HTML（含普通 img + 穿透 shadow-root 抓回的柏宝绘图）。
- 阅读页用 `cleanFavHtml(rendered)` 清洗（剔 style + 去重复引号）后渲染。
- 导出/注入时常用 `mes` 或 `rendered→去标签` 取文本。

---

## 十四、主人常见需求 → 直接操作速查

| 主人说 | 你要做什么 |
|---|---|
| 面板太小/太大/想更宽 | 改 `ensureRoot` + `favRenderView` 两个 `width:xx;height:xx`（同步） |
| 收藏界面样子不对 | 改 `style.css` 的 `#th-favroot` 系；或 `favRenderView` |
| 拍照/复制按钮位置 | `favEnsureBtns` 里 right 值 |
| 长按误触/点了弹收藏 | 查 `__wireLongPressGlobal` 是否在；若被删就补回 |
| 注入/删除没反应 | 弹窗是否用了 `favConfirmTop`（顶层）；别用 thConfirm |
| 收藏图丢了 | `grabMesHtml` shadow-root 穿透 |
| 设置保存当前组/建立新组 | `saveCurrentGroup` / `buildNewGroup` |
| 替换名称退出没了 | `__thDraft` 草稿机制 |
| 面板尺寸统一 | 两处 480×500 同步 |
| 发布到 GitHub | 走第十一节 REST API |
| 想改社区介绍 | 改 `README.md`（功能清单 + 每项说明，零 emoji） |
| 给它写文档 | 更新开发日志/接手总纲 |

---

## 十五、已完成功能清单（避免下个 AI 重复做）

- [x] 分组/条目/收藏（fav 星标）
- [x] 宏替换（{{user}}/{{char}} 三形态）
- [x] 两套替换组（全局+聊天）+ 存成组 + 自定义替换
- [x] 发送/填框
- [x] 详情编辑
- [x] 导入导出 JSON + miniStage 转换
- [x] 收藏聊天记录：长按 / ★ / 写标题小窗
- [x] 收藏查看界面（按角色分组折叠 + 阅读页）
- [x] 收藏多选：删除 / 导出HTML / 注入到聊天
- [x] 设置页：导出ZIP / 导入ZIP / JSON备份
- [x] 消息右下角角标：★收藏(50) ✓收录(2) 复制(74) 拍照(98)
- [x] 单条拍照→竖屏PNG全文字 + 兜底
- [x] 单条复制
- [x] 可拖动悬浮球 + 记忆位置
- [x] 面板统一 480×500 更宽更矮
- [x] 长按防 AI 生成误触（document级兜底+滚动抑制）
- [x] 设置「保存当前组」「建立新组」
- [x] 移除收藏界面顶部筛选胶囊
- [x] README 功能清单式社区介绍（零 emoji）
- [x] 开发日志/开发介绍/开发经历与问题处置 文档
- [x] 上传 GitHub（小剧场 + 一切皆可收纳）

---

## 十六、style.css 关键点

- 顶部 `:root{}` 定义 CSS 变量：`--th-bg --th-panel --th-card --th-line --th-ink --th-accent --th-gold --th-tag` 等。改配色先看这里。
- 收藏界面走 `#th-favroot` / `#th-favwin`，用主界面变量，保持一套皮肤。
- `q{quotes:none}` 防 `<q>` 自动加引号（配合 cleanFavHtml）。
- `summary::-webkit-details-marker{display:none}` 让思维链折叠箭头正常。
- 全 SVG、无 emoji。

---

*（本部分为补充，与 Part1/Part2 合并为完整总纲）*
