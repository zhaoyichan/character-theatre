# 小剧场（character-theatre）· 独立ST插件 · 交接文档

> **本文件是给下一个接手的开发者/我(AI)的完整交接底稿，覆盖：**
> 1. 插件是什么、长什么样、装了哪些东西
> 2. 所有已做功能与如何工作
> 3. 所有踩过的坑、根因、当时的解法
> 4. 尚未完成/待办，以及主人的真实意图
>
> **读法**：按顺序读。遇到「⚠️坑」标记，务必记牢，别重复踩。
> 本文件与《小剧场-独立ST插件-全量移交文档.md》结合读。

---

## 检查清单（新人接手第一件事）
- [ ] 运行目录在哪、开发目录在哪、三处一致性怎么核
- [ ] manifest 必须有 author，否则酒馆拒载
- [ ] 改 JS 必须 `node --check`
- [ ] 改完必须**完全冷启动酒馆**（force-stop + 重启）才生效
- [ ] 双引号、思维链折叠、zip、长截图这四块是"血泪区"，先读

---

## 1. 插件是什么

独立 SillyTavern 第三方插件，提供「**小剧场**」：
- 按分组存多条"小剧场/对白片段"
- 发到聊天时自动把 `{{user}}`/`{{char}}` 等占位符替换成设置的称呼
- 每条可「发送」进聊天气泡 / 「填框」进输入框
- 详情编辑（标题 + 大文本 content）
- 收藏聊天记录（把看中的聊天消息收进收藏夹）
- JSON/ZIP 导入导出备份

**命名**：display_name「小剧场」；插件名/目录 `character-theatre`。

---

## 2. 目录位置（务必记清）

| 用途 | 路径 |
|---|---|
| **运行目录（生效，需 shell 权限写）** | `/storage/emulated/0/Android/data/com.tauritavern.client/data/extensions/third-party/character-theatre/` |
| **开发镜像目录（改这里或用 tools 编辑）** | `/sdcard/Download/酒馆插件开发/酒馆小剧场开发/character-theatre/` |

> ⚠️ 运行目录是**真身**，改完必须同步到开发目录，并且**完全冷启动酒馆**（force-stop+重启）才加载新扩展。

**关键文件**：
- `manifest.json`（含 `author`，缺失会整包拒载）
- `index.js`（约 1800 行，全部逻辑）
- `style.css`（约 250 行，浅色护眼、全 SVG、无 emoji）
- `开发日志.md`（版本迭代更新记录）
- `小剧场-交接文档.md`（本文件）

---

## 3. 已做功能清单

### 3.1 分组 / 条目 / 收藏
- 平面分组（groups: `[{name, items:[{title, content, fav}]}]`）
- 每条小剧场：标题 + 正文（可含 `{{user}}`/`{{char}}`）+ 收藏星
- **收藏**：每条消息右下角有 ★ 收藏按钮 + 长按可收藏；收藏存 `th-fav-store`，跨聊天全局
- 收藏查看界面：按角色卡分组、可折叠、可查看详情、多选删除

### 3.2 宏替换
- `{{user}}` / `<user>` / `user` → 用户名
- `{{char}}` / `{{chat}}` / `<chat>` / `char` → 角色名
- 支持全局替换组 + 聊天记录替换组 两组独立，可存成组自由切换
- 自定义替换行「＋ 新增替换」：组级 `extra:[{from,to}]`

### 3.3 发送 / 填框
- **发送** = 作为用户发言 push 进 `ctx.chat` + saveChat + reload + emit MESSAGE_SENT（**不触发 AI 自动生成**，见坑）
- **填框** = 只填进 `#send_textarea`，不自动发

### 3.4 编辑 / 详情
- 条目列表只显示标题
- 点标题进详情编辑（标题输入 + 大 textarea 内容，保存/发送/返回）

### 3.5 导入 / 导出
- 收藏 + 分组均可导出备份
- **外插件 miniStage 转换**：识别 `_miniStage/prompts[]` 格式并按 groupId 归组
- **ZIP 导出/导入**（设置页）：把收藏打包 zip（index.html + data.json）
- **多选批量导出 HTML**（收藏界面）：图片 base64 内嵌

### 3.6 收藏界面
- 居中 modal 高窗（覆盖小剧场之上，层级更高）
- 按角色分组折叠文件夹（色点 + 组名 + 条数 + 箭头）
- 顶部筛选胶囊
- 多选 → 删除 / 导出 / **注入到聊天**

### 3.7 右下角新按钮（酒馆聊天界面右下角）
- 悬浮球 + 拍照(长截图) + 复制(剪贴板)，垂直一列并列
- 拍照当前导出 SVG（待改 PNG/JPEG），复制可用
- ⚠️ 这两个按钮应在**酒馆聊天界面的右下角**，不是收藏界面

---

## 4. 数据结构（localStorage / 键）

```js
KEY_DATA = 'th-theatre-data';         // 分组条目 [{name,items:[{title,content,fav}]}]
KEY_MAP_GLOBAL = 'th-map-global';     // 全局替换组 {groups:[{name,user,char,extra?}],current}
CHAT_MAP_FIELD = '__theatreChatMap';  // 聊天记录替换组（随聊天，new chat 为空）
FAV_KEY = 'th-fav-store';             // 收藏 {v:1, items:[{id,role,chatTitle,startFloor,endFloor,time,note,title,msgs:[...]}]}
```
- `rendered` = 消息原始 HTML（含 img/details），收藏时原样保存
- 内存权威缓存 `_cache`：读写先过它，避免 localStorage 抖动丢数据

---

（待续：第 5 节函数索引、第 6 节踩过的坑、第 7 节待办等）