# 小剧场 character-theatre · 交接总纲（全量快照，供下一位开发者）

> 本文件为 **2026-09-04** 全量交接快照。**下一位开发者务必先读本文件**，并配合 `开发日志.md` 的「更新目录」追踪版本。
> 上下文触顶在即，此文件力求一次写全：当前全部需求、已实现、待办、踩坑与解法、关键数据与文件位置。

---

## 〇、这是什么 / 一句话

独立 SillyTavern 第三方插件「**小剧场**」。
- 作用：把想复用的对白/桥段按**分组**存成条目，发到聊天时把 `{{user}}`/`{{char}}` 等占位符按设置替换成称呼后**发送**或**填框**。
- 也支持 **收藏聊天记录**（长按/点★），并把收藏按角色分组查看、删除、导出（HTML / ZIP）。
- 独立自给自足，不依赖任何宿主插件，入口是**右下角悬浮球**。

插点名：`character-theatre`；显示名：小剧场；作者：`liwe和他的后宫老婆们`。

---

## 一、文件位置（三处必须同步）

| 用途 | 路径 |
|---|---|
| **运行目录（生效，需 shell/root 写）** | `/storage/emulated/0/Android/data/com.tauritavern.client/data/extensions/third-party/character-theatre/` |
| **开发副本（改这＋同步回去）** | `/storage/emulated/0/Download/酒馆插件开发/酒馆小剧场开发/character-theatre/` |
| **需求铁律文件（改前先读）** | `/storage/emulated/0/Download/酒馆插件开发/liwe记忆/需求-小剧场功能.md` |
| **参考插件（收藏实现可抄）** | 运行目录 `.../third-party/SillyTavern-Plugin-Harvest/` + 开发目录 `酒馆插件开发/一切皆可收纳/` |

- **副本里工具文件：** `index.js`（全部逻辑）、`style.css`（全部样式）、`manifest.json`、`开发日志.md`、本文件。
- **运行目录读写注意事项**：Android 的 data 目录普通 terminal 读不了（Permission denied），必须用 **`super_admin:shell`（root/Shizuku）cp**；node --check 需先 cp 到 `/sdcard/Download/` 再跑。
- 改完必须**完全冷启动酒馆**（划掉重开）才加载新扩展。

---

## 二、当前需求全量清单（按主人最新口径）

### 界面 / 交互既有要求
- 入口：右下角**悬浮球**（单点打开小剧场面板）。
- 首页/组内都是**单列流卡片**，全 **SVG 图标、零 emoji**、紧凑偏小、浅色护眼。
- 首页 = 组列表（图标+组名+条数+右箭头，点进组）；组内 = 条目列表（只显示标题，点标题进详情编辑）。
- 组内详情编辑页：标题 + 大文本域正文 + 保存/发送/✕返回。
- 顶部 action 区按钮：**主页** = `✓多选` `+新建组` `⚙设置`，左 `✕回酒馆`；标题旁有金色 **▶收藏**图标（紧贴「小剧场」字样）。
- 宏替换设置：全局 / 聊天记录 两组独立，可存成组自由切换，`＋新增替换`自定义词→词。
- 导入/导入：小剧场分组 JSON；外插件小剧场 miniStage 格式可转换导入。

### 收藏聊天记录功能
- **触发**：长按消息 或 点消息右下角 **★** 按钮 → 弹「收藏这一条」**写标题**小窗 → 确认收藏（标题备注）。
- 收藏数据存 `th-fav-store`（localStorage + 兜底扩展设置）。读取原样 HTML（含图片、shadow-root 里的柏宝绘成品图）。
- **查看收藏**：小剧场首页「小剧场」旁 **▶（folder 金色钮）** 进收藏界面。按角色分组折叠文件夹。
- 阅读页：`.hv-novel` 结构（note/meta/楼层/对话），正文渲染原始 HTML+图片，支持 details 折叠块点开、引号清洗。
- ✕ 关闭收藏 = 连带关小剧场回酒馆；← 返回 = 回小剧场。

### 设置页底部（备份与恢复卡）
- 现有：`导出备份(JSON)` `导入备份(JSON)` `清空重置`
- **已加：`导出ZIP` `导入ZIP`**——导出收藏全部为 `.zip`（内含 index.html 可预览 + data.json 可恢复）；导入读取 zip 里的 data.json 恢复收藏。

### 收藏多选（需要补齐）
- 多选里有 `删除` + **`导出HTML`**（勾选条目导出为一个内嵌 base64 图片的独立 html）——导出HTML 已做。
- **主人要求：多选里加「注入到聊天」**——把选中的收藏当作消息发回当前聊天 → **尚未做**。

### 聊天界面右下角并列按钮（新要求，刚做）
- 位置：**酒馆聊天的右下角**，与悬浮球并列（right:16px，垂直排开）。
- 悬浮球（bottom:90）下方依次：
  - 拍照（bottom:144，相机SVG）→ 长截图。
  - 复制（bottom:198，复制SVG）→ 复制**当前这一条**聊天消息。
  - **注意**：主人明确「点这些按钮=复制/拍照的是**这一条消息**，不是整段」。当前 `thCopyChat`/`thCaptureChat` 是整段聊天，**需改成针对单条消息**——见「待办」。

### 收藏界面皮肤
- 主人要求收藏界面**照小剧场皮肤来**（不是现在白色卡片风）。小剧场真实皮肤在运行目录 `style.css` 里（浅米白 `#f6f5f1`、面板 `#fbfaf7`、白卡、圆角16、深藏青、金）。**待对照重做**。

---

## 三、已实现的函数 / 结构速查（照名字搜）

数据层：`getGroups/saveGroups/findGroup/safeItems/getGlobalMap/saveGlobalMap/getChatMap/saveChatMap/grabText/applyReplace/resolveNames/pickPicked/favLoad/favPersist/favRoleName/favChatName/favRoleColor`
收藏：``
入口/右下角：`registerEntry/thAddCorner/iconCam/iconCopy/thCaptureChat/thCopyChat`
UI：`topBar/topIcon/renderHome/renderGroupView/renderItemDetail/renderSettingsView/chkLine/reg/runH/thModal/thPrompt/thConfirm/thSquare/favChromelike…`
诊断：`logEvent/renderDebugBar/toggleDebugBar`

### 数据键
- `th-theatre-data`：分组/条目 `[{name,items:[{title,content,fav}]}]`
- `th-map-global`：全局替换组 `{groups:[{name,user,char,extra?}],current}`
- 聊天替换组：挂 chat 上 `__theatreChatMap`
- `th-fav-store`：收藏 `{v:1,items:[{id,role,chatTitle,startFloor,endFloor,time,note,title,msgs:[{name,is_user,role,mes,rendered,floor}]}]}`
- 内存权威缓存 `_cache`：读写先过它。

---

## 四、踩过的坑与解法（血泪，务必记）

1. **安卓 WebView 禁 `prompt/confirm`** → 全部用自绘弹窗（`thPrompt`/`thConfirm`/`thModal`/`thSquare`/`favPrompt`）。
2. **数据污染 bug**：`newItem/toggleFav/…` 曾误传**单个组对象**给存整组数组的 `saveGroups`，致 localStorage 被污水对象、组一更新就消失。解：`saveGroups` 只收数组，改动存整组。
3. **`gs.reduce is not a function`**：localStorage 里存了非数组 → `getGroups()` 强制 `Array.isArray ? :[]` 归一化。
4. **弹窗挂载到 `getPanel()` 导致在聊天主界面弹不出** → 收藏标题窗、双击收录窗**固定挂 `document.body` + 高 z-index**。
5. **收藏二次添加失败** = 标题弹窗 `thPrompt` 挂错了容器/层级 → 新增 `favPrompt`（fixed 顶层挂 body）。
6. **双击收录偶发失效** → 用原生 `addEventListener`（不用可能不存在的 jQuery `.on`）。
7. **收藏后图片丢失**：普通 `innerHTML` 抓不到渲染在 **shadow-root** 里的柏宝绘成品图 → `grabMesHtml` 穿透 `data-bbi-slot` 锚点 shadowRoot 抓 img 并原位插回。
8. **对话双引号变两对**（`""…""`）：
   - **`<q>` 标签会被浏览器自动加引号** → 必加 CSS 禁语义引号：`q::before/::after{content:none!important} q{quotes:none}`。
   - 加上 `cleanQuotes` 两步：单文本节点内收敛 + **相邻文本节点边界**同向引号去首。
9. **思维链 `<details>` 折叠点不开/没箭头**：要加 `summary::-webkit-details-marker{display:none}` + `summary::after` 箭头 + `details[open]>summary::after` 旋转。
10. **zip 手写**已用 Python 标准 zipfile 验证可解压（crc32 + 目录 + EOCD 正确）。
11. **下载的 svg 在相册看不到**：主人反馈「拍照下载 svg 看不了」，需换 **PNG**（canvas.toDataURL('image/png')）或桥接原生保存相册。
12. **运行目录 terminal 写不了**：必须 `super_admin:shell`。

---

## 五、待办（下一位开发者按此推进）

1. **右下角拍照/复制 → 针对“单条消息”**：主人明确是复制/拍照**这一条**（点哪条哪条），且拍照格式换成 **PNG**（或能进相册的方式）。当前 `thCopyChat`/`thCaptureChat` 是整段+SVG，需改。
2. **收藏界面皮肤 → 照小剧场风格重做**（对照运行目录 `style.css` 小剧场主界面皮肤）。
3. **收藏多选加「注入到聊天」**：选中的收藏发回当前聊天（复用 `sendText/grabText`）。
4. 理顺 `favChromelike` / 收藏查看与收藏界面的尺寸、皮肤统一问题（主人一直在追「尺寸要和面板一致」+「皮肤按小剧场」）。

---

## 六、如何验证

- 每次改：`node --check`（先 cp 到 `/sdcard/Download`）+ 冷启动酒馆看效果。
- 改完同步三处：开发往返 + 运行目录。
- 大改动前 `cp index.js index.js.bak_原因` 备份。

---

*本文件随新进展同步更新；优先级高于对话记忆。*
